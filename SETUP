1. Clone i3, compton and dotfiles repo
2. Execute setup.sh to install all dependencies
3. make && sudo make install for i3
5. use dconf-editor, navigate to org/gnome/settings-daemon/plugins/cursor and untick "active"
